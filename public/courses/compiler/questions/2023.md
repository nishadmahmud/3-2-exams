# 2023

**1. (a) Draw the block diagram of the phase of a compiler.** [2]
> **Answer:**
> 

**(b) How the phases of a compiler can be grouped? How does the Lexical Analyzer increase the efficiency of the compiler?** [2+2]
> **Answer:**
> 

**(c) Show output of different phases to translate the C statement `c = a+b*60` where a, b and c are variables.** [4]
> **Answer:**
> 

**2. (a) What is the purpose of assembler, linker and loader in compiler?** [3]
> **Answer:**
> 

**(b) What language is generated by the following grammars? In each case justify your answer.** [2.5]<br>
i. $S \rightarrow 0S1 \mid 01$<br>
ii. $S \rightarrow +SS \mid -SS \mid a$<br>
iii. $S \rightarrow S(S)S \mid \epsilon$<br>
iv. $S \rightarrow aSbS \mid bSaS \mid \epsilon$<br>
v. $S \rightarrow a \mid S+S \mid SS \mid S^* \mid (S)$
> **Answer:**
> 

**(c) Which of the grammars in Question 2(b) are ambiguous?** [2.5]
> **Answer:**
> 

**(d) Consider the set with strings on $\{0,1\}$. Now construct a DFA and NFA that accepts.** [2]<br>
i) All strings contain 00 but not 01.<br>
ii) All strings containing no occurrence of 01.
> **Answer:**
> 

**3. (a) What is a regular expression? In which part is it necessary in Compiler design?** [1+1]
> **Answer:**
> 

**(b) Consider the following CFG.** [4]<br>
**Productions:**<br>
$E \rightarrow E+T \mid T$<br>
$T \rightarrow T*F \mid F$<br>
$F \rightarrow (E) \mid id$<br>
**What will be the output of FIRST () and FOLLOW () functions for the above CFG?**
> **Answer:**
> 

**(c) Construct the LL (1) parsing table for the following grammar.** [4]<br>
**Productions:**<br>
$E \rightarrow E+T \mid T$<br>
$T \rightarrow T*F \mid F$<br>
$F \rightarrow (E) \mid id$
> **Answer:**
> 

**4. (a) Show that all binary strings generated by the following grammar have values divisible by 3.** [2]<br>
*Hint. Use induction on the number of nodes in a parse tree.*<br>
$num \rightarrow 11 \mid 1001 \mid num\ 0 \mid num\ num$
> **Answer:**
> 

**(b) Does the grammar in question 4(a) generate all binary strings with values divisible by 3?** [2]
> **Answer:**
> 

**(c) Construct a syntax-directed translation scheme that translates arithmetic expressions from postfix notation into infix notation. Give annotated parse trees for the inputs `95-2*` and `952*-`.** [4]
> **Answer:**
> 

**(d) Construct CFG for the language having any number of a's over the set $\Sigma=\{a\}$.** [2]
> **Answer:**
> 

## PART-B
*(Answer any three questions)*

**5. (a) We can prove that two regular expressions are equivalent by showing that their minimum-state DFA's are the same, except for state names. Using this technique, show that the following regular expressions are all equivalent.** [3]<br>
i. $(a \mid b)^*$<br>
ii. $(a^* \mid b^*)^*$<br>
iii. $((\epsilon \mid a)b^*)^*$
> **Answer:**
> 

**(b) Consider the following FA and construct a minimum state FA for given finite automata.** [7]<br>

| State | 0 | 1 |
| :---: | :---: | :---: |
| $\rightarrow q_1$ | $q_2$ | $q_6$ |
| $q_2$ | $q_7$ | $q_3$ |
| $*q_3$ | $q_1$ | $q_3$ |
| $q_4$ | $q_3$ | $q_7$ |
| $q_5$ | $q_8$ | $q_6$ |
| $q_6$ | $q_3$ | $q_7$ |
| $q_7$ | $q_7$ | $q_5$ |
| $q_8$ | $q_7$ | $q_3$ |

> **Answer:**
> 

**6. (a) Consider the grammar $S \rightarrow aSbS \mid bSaS \mid \epsilon$** [1x3]<br>
i. Show that this grammar is ambiguous by constructing two different leftmost derivations for the sentence abab.<br>
ii. Construct the corresponding rightmost derivations for abab.<br>
iii. Construct the corresponding parse tree for abab.
> **Answer:**
> 

**(b) Try to design grammar for each of the following languages. Which languages are regular?** [2]<br>
i. Strings of 0's and 1's with an equal number of 0's and 1's.<br>
ii. Strings of 0's and 1's with an unequal number of 0's and 1's.<br>
iii. Strings of 0's and 1's of the form xy where $x \neq y$.<br>
iv. Strings of 0's and 1's of the form xx.
> **Answer:**
> 

**(c) Construct the SLR parsing table for this grammar.** [5]<br>
$E \rightarrow E+T \mid T$<br>
$T \rightarrow T*F \mid F$<br>
$F \rightarrow F^* \mid a \mid b$
> **Answer:**
> 

**7. a) What is Three Address Code? Explain the concepts to build Three Address Code.** [2]
> **Answer:**
> 

**b) Define Quadruples, Triples and Indirect Triples.** [3]
> **Answer:**
> 

**c) Construct the DAG for the expression `((x+y)-((x+y)*(x-y)))+((x+y)*(x-y))` and convert the corresponding Three Address Code.** [5]
> **Answer:**
> 

**8. (a) Generate optimal code for the following assignment statements:** [2]<br>
i. `x := a + b + c`<br>
ii. `x := (a * - b) + ( c - (d + e) )`<br>
iii. `x := ( a / b - c ) / d`<br>
iv. `x := a + ( b + c / d * e ) / ( f * g - h * i )`<br>
v. `a[i, j] := b[i, j] - c[ a[ k, l] ] * d[i + j]`
> **Answer:**
> 

**(b) Consider the followings three-address instructions:**<br>
1. `i=1`<br>
2. `j=1`<br>
3. `t1=5*i`<br>
4. `t2=t1+j`<br>
5. `t3=4*t2`<br>
6. `t4=t3`<br>
7. `a[t4]=-1`<br>
8. `j=j+1`<br>
9. `if j<=5 goto (3)`<br>
10. `i=i+1`<br>
11. `if i<5 goto (2)`<br><br>
i. Partition the followings three-address instructions into basic blocks<br>
ii. Construct a flow graph from the three-address statements.<br>
iii. What is the depth of the flow graph?
> **Answer:**
> 

**(c) Generate code for the following C statements:**<br>
i. `x= f(a) + f(a) + f(a)`<br>
ii. `x= f(a)/g(b,c)`<br>
iii. `x=f(f(a))`<br>
iv. `x=++f(a)`
> **Answer:**
> 
