# 2022

**1. (a) Describe the role of lexical analyzer. Why is analysis portion of compiler normally separated into lexical analysis and syntax analysis?** [1+1]
> **Answer:**
> 

**(b) Identify the lexemes and tokens from the following C statement where i, j and max are variable. Give attribute values for the tokens.** [2]<br>
`if(i>=j) max=i;`<br>
`else max=j;`
> **Answer:**
> 

**(c) What kind of errors syntax analysis & semantic analysis detect? From the below source program find out lexical error, syntax & semantic errors-** [1+1]
```c
int * token (int i, int j, k)){
    int x;
    for(i=0; i j)
    fi(i>j)
    printf("Hello);
    ;
    x=x+j+N;
    return j;}
```
> **Answer:**
> 

**(d) During compilation show the output of each phase of a compiler for the below statement:** [4]<br>
`Res+=b*10`
> **Answer:**
> 

**2. (a) Consider the following grammar. $S \rightarrow S\ S\ + \mid S\ S\ * \mid a$ and string $aa+a^*$.** [4+2]<br>
i) Give a left most & right most derivation for the string.<br>
ii) Construct a parse tree for the string.
> **Answer:**
> 

**(b) Convert the following NFA into DFA** [4]<br>
![NFA Diagram](/courses/compiler/images/2022_q2b_nfa.svg)

> **Answer:**
> 

**3. (a) What is the role of the parser?** [2]
> **Answer:**
> 

**(b) Consider the grammar** [2+2]<br>
$S \rightarrow (L) \mid a$<br>
$L \rightarrow L,S \mid S$<br>
Find parse trees for the following sentences:<br>
i) $(a, (a, a))$<br>
ii) $(a, ((a, a), (a, a)))$
> **Answer:**
> 

**(c) Consider the following grammar and construct the operator precedence parser.** [4]<br>
$E \rightarrow E A E \mid id$<br>
$A \rightarrow + \mid *$<br>
Then parse the following string: `id+id*id`
> **Answer:**
> 

**4. (a) Identify the types of grammar(Linear, Regular, Context-free) form the following:** [5]<br>
i) $S \rightarrow aSa \mid bSb \mid \lambda$<br>
ii) $S \rightarrow abB$, $B \rightarrow abB \mid \lambda$<br>
iii) $S \rightarrow Aa$, $A \rightarrow bA$, $B \rightarrow A$, $B \rightarrow \lambda$<br>
iv) $S \rightarrow AbbA \mid aaA$, $A \rightarrow S \mid \lambda$<br>
v) $S \rightarrow A$, $A \rightarrow aA \mid \lambda$
> **Answer:**
> 

**(b) Consider the following grammar:** [1+1]<br>
$S \rightarrow i\ C\ t\ S \mid i\ C\ t\ S\ e\ S \mid a$<br>
$C \rightarrow b$<br>
i) Construct a **left most derivation** for the sentence "i b t i b t a e a"<br>
ii) Construct a **right most derivation** for the sentence "i b t a e i b t a"
> **Answer:**
> 

**(c) Eliminate left recursion from and perform left factoring on the following grammar.** [3]<br>
$E \rightarrow E+T \mid T$, $T \rightarrow T F \mid F$, $F \rightarrow F^* \mid I$, $I \rightarrow a \mid b$
> **Answer:**
> 

## PART-B
*(Answer any three questions)*

**5. (a) Consider the context free grammar: $E \rightarrow E+T \mid T$, $T \rightarrow T*F \mid F$, $F \rightarrow (E) \mid id$** [4+6]<br>
i) Eliminate the left recursion<br>
ii) Compute FIRST and FOLLOW for the generated non left recursive grammar.
> **Answer:**
> 

**6. (a) Draw the model of LR parser. Write the LR parser algorithm.** [1+2]
> **Answer:**
> 

**(b) (i) Show that the following grammar** [1+1]<br>
$S \rightarrow AaAb \mid BbBa$<br>
$A \rightarrow \epsilon$<br>
$B \rightarrow \epsilon$<br>
Is LL(1) but not SLR(1).<br>
(ii) Show that every LL(1) grammar is an LR(1) grammar
> **Answer:**
> 

**(c) Consider the following grammar** [5]<br>
$E \rightarrow E+T \mid T$<br>
$T \rightarrow T*F \mid F$<br>
$F \rightarrow (E) \mid id$<br>
Construct the SLR parsing table for this grammar.
> **Answer:**
> 

**7. (a) Translate the executable statement of the following C program** [3]<br>
```c
main()
{
    int i;
    int a[10];
    i=1;
    while(i<=10){
        a[i]=0; i=i+1;
    }
}
```
Into<br>
i. A syntax tree<br>
ii. Postfix notation<br>
iii. Three-address code.
> **Answer:**
> 

**(b) Generate code for the following C statements for the target machine, assuming all variables are static. Assume three registers are available.** [5]<br>
i. `x=1`<br>
ii. `x=y`<br>
iii. `x=x+1`<br>
iv. `x=a+b*c`<br>
v. `x=a/(b+c)-d*(e+f)`
> **Answer:**
> 

**(c) Suppose parameters are passed by value instead of by reference. Can two names be aliases of one another? What if copy-restore linkage is used?** [2]
> **Answer:**
> 

**8. (a) Construct DAG, Three address code, Quadruple and Triple for the expression** [4]<br>
`((x+y)-((x+y)*(x-y)))+((x+y)*(x-y))`
> **Answer:**
> 

**(b) Write an algorithm to partition a sequence of three-address statements into basic blocks.** [2]
> **Answer:**
> 

**(c) Write Machine Instruction code for the following 3 address code** [4]<br>

| (i) | (ii) | (iii) |
| :--- | :--- | :--- |
| `x=a[m]`<br>`y=b[n]`<br>`a[m]=y`<br>`b[n]=x` | `y=*q`<br>`q=q+4`<br>`*p=y`<br>`p=p+4` | `i=5`<br>`L1: if i<1 goto L2`<br>`i=i-1`<br>`goto L1`<br>`L2:` |

> **Answer:**
> 
